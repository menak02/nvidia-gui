"""nvidia-gui — a Linux-native, open-source control center for NVIDIA GPUs.

A GTK4 front-end that exposes the parts of the Windows "NVIDIA App" that have
honest Linux equivalents: per-game launch-option overrides (DLSS presets,
NVAPI, Reflex, MangoHud), opt-in DLSS DLL swaps from a managed cache, live GPU
telemetry via nvidia-smi, read-only driver/display info, and persistent global
driver tuning via NVreg_* kernel-module parameters.
"""

__version__ = "1.0.0"
